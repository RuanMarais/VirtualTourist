//
//  File.swift
//  Virtual Tourist
//
//  Created by Dr GJK Marais on 2016/12/12.
//  Copyright © 2016 RuanMarais. All rights reserved.
//

import Foundation
/*
if deleteDictionary.count != 0 {
    
    FlickrClient.sharedInstance.loadPhotoCoreDataForPin(pin: pin!, context: context, replacementNumber: deleteDictionary.count){(success, error) in
        performUIUpdatesOnMain {
            if success {
                for (_, value) in self.deleteDictionary {
                    context.delete(value)
                }
                self.stack.save()
            }
            
        }
        
    }
    
} else {
    
    
    FlickrClient.sharedInstance.loadPhotoCoreDataForPin(pin: pin!, context: context, replacementNumber: nil){(success, error) in
        performUIUpdatesOnMain {
            if success {
                for photo in self.pin!.pinPhotos! {
                    if let photo = photo as? CollectionPhoto {
                        context.delete(photo)
                    }
                }
                self.stack.save()
            }
            
        }
    }
}
*/
